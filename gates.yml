# The gate register. Every check in this organisation has an ID here, and this
# file is the only place a check is defined.
#
# WHY IT IS A FILE AND NOT A SECTION OF THE PLAN. Nine `continue-on-error`
# suppressions accumulated in this org, and four gates were found that could
# not fail at all — one audited its own repository, one printed "passing
# trivially" on an empty manifest, one greped a PDF field while 432 PDFs
# shipped missing a different one, and one always skipped. None of that was
# dishonest. Each was a check whose definition lived in the workflow that ran
# it, so nothing outside that workflow could notice it had stopped meaning
# anything. A register in prose has the same defect; this one is read by
# `bin/kit` to build the battery and by `verify_gate_register.py` to check
# itself, so a gate that drifts from its description fails something.
#
# FIELDS
#   id        A1–A19 source · B1–B4 build · C1–C15 rendered · D1–D6 artefacts
#             · E1 deploy. IDs are permanent and never reused. A withdrawn
#             gate becomes status: withdrawn and keeps its number.
#   owner     which repo holds the implementation: kit, .github, curriculum
#   scripts   [] while planned. More than one is allowed where an ID's
#             contract genuinely has separable halves (A1, A12).
#   needs     what must exist before it can run:
#             source · built · materials · audio · none
#   severity  blocking | warn
#   ramp      REQUIRED when severity is warn. A warn gate with no route to
#             blocking is a suppression wearing a gate's clothes, and the
#             register verifier fails on one. This field is the difference
#             between "not yet" and "never".
#   status    live | partial | planned | withdrawn
#             partial REQUIRES `gap:` — what the ID promises and the script
#             does not yet do. An ID marked live whose script does half the
#             job is how a battery grows a hole nobody can see.
#   booked    REQUIRED when planned: the phase that delivers it.
#   battery   true if `kit check` runs it. A live gate outside the battery
#             must say where it runs instead, in `runs`.

version: 1
framework_pin: null   # A16 reads its thresholds from the curriculum checkout,
                      # never from a constant here — see S2's coverage rule.

gates:

# ── Stage A · source, before Hugo ────────────────────────────────────────────

- id: A1
  title: kit drift
  owner: .github
  scripts: [scripts/kit_drift.py, kit:scripts/verify_kit_lock.py]
  needs: source
  severity: blocking
  status: partial
  gap: >-
    Covers kit-owned code in a course, step-level continue-on-error, `|| true`
    on a gate command, the _materials/ digest lock, deploy.yml byte-equality
    with the template, and the absence of any second workflow beside the
    caller. Does NOT yet check the hugo.toml key allowlist or an override's
    kit_base minor distance.
  note: >-
    Until kit v1.21.0 this gate examined nothing in the battery. It reads its
    argument as an ORG checkout and iterates the directories under it; `kit
    check` hands every gate the REPO, so the "courses" were content/, static/
    and data/, none of them imported the kit, and it printed "0 course(s)
    checked — A1 OK". Green on all five, having compared nothing. It was
    invisible for as long as the gate register was aborting before the battery
    ran at all, which is the general shape of this org's gate failures: the
    outer fault hides the inner one.
  battery: true

- id: A2
  title: front matter and curriculum block
  owner: kit
  scripts: [scripts/verify_curriculum_block.py]
  needs: source
  severity: warn
  ramp: >-
    Severity is read per repo from `milestone:` in boulingua.yml, not decided
    per pull request: M0 warns, M1 requires it on touched pages, M2 repo-wide,
    M3 adds coverage. Malformed and too-narrow ids fail at every milestone —
    only the missing-block requirement ramps, because an id that is present
    must be right.
  status: partial
  gap: >-
    Enforces implements, its id grammar and the breadth rule. Does not yet
    validate the full page schema (page_type, unit_nr, material_slug, closed
    enums, asset resolution) — that arrives with the JSON Schema in F9.
  battery: true

- id: A3
  title: VG Wort URL lock
  owner: kit
  scripts: [scripts/vgwort_urldiff.py, scripts/vgwort_lock.py]
  needs: built
  severity: blocking
  status: partial
  gap: >-
    Runs in NEUTRALITY MODE. It proves a change moved no URL. It cannot prove
    the URLs it defends are the ones registered at VG Wort, because the lock is
    derived from the built sitemap and no T.O.M. export exists yet. The
    filename says so — url-lock-provisional.csv. Becomes complete at 15.4.
  battery: false
  runs: pre-merge on the three live courses; blocking there since P0.8

- id: A4
  title: licence set
  owner: kit
  scripts: [scripts/verify_fonts.py]
  needs: source
  severity: blocking
  status: partial
  gap: >-
    The font half is done: every shipped .woff2 traced to a licence and an
    ATTRIBUTION entry. The repo half — LICENSE, LICENSE-CONTENT.md, NOTICE.md,
    REUSE.toml, CITATION.cff — is not checked yet.
  battery: true

- id: A5
  title: source placeholders
  owner: kit
  scripts: [scripts/verify_legal_placeholders.py]
  args: ["--source"]
  needs: source
  severity: blocking
  status: live
  battery: true

- id: A6
  title: Quarto residue
  owner: kit
  scripts: []
  needs: source
  severity: blocking
  status: planned
  booked: P3.3
  battery: false

- id: A7
  title: design tokens
  owner: kit
  scripts: [scripts/verify_tokens.py]
  needs: source
  severity: blocking
  status: live
  battery: true

- id: A8
  title: logical CSS
  owner: kit
  scripts: [scripts/verify_logical_css.py]
  needs: source
  severity: blocking
  status: live
  battery: true

- id: A9
  title: chrome i18n
  owner: kit
  scripts: [scripts/verify_i18n_chrome.py]
  needs: source
  severity: blocking
  status: live
  battery: true
  note: >-
    Its own ramp condition is met: kit/i18n/de.yaml exists and covers every key
    the layouts reference, so the gate that was waiting for it is blocking.

    It found six German literals in the KIT's own layouts on its first run —
    Foliensatz, Arbeitsblatt, Herunterladen, Downloads, and the aria-labels
    Hauptmenü and Farbschema — which every course inherits through the module.
    The aria-labels are the ones that matter most: they are what a screen
    reader speaks, so a Greek course was announcing its menu in German.

- id: A10
  title: contrast and accent separation
  owner: kit
  scripts: [scripts/verify_contrast.py]
  needs: source
  severity: blocking
  status: live
  battery: true
  note: >-
    Its ramp condition is met: S5 re-derived the palette, so the gate that was
    waiting for it is blocking. Ten of eighteen accents were below 4.5:1 on
    white and eight pairs sat under 15°; the worst figures now are 4.62:1 and
    16.4°.

    It measures tokens.yaml, not the round-tripped hex, and that distinction
    earned itself during S5: the solver converged on 17° of separation, wrote
    8-bit hex, and read back 11.3°. Darkening a saturated colour at constant
    chroma walks it out of the sRGB gamut and clamping the channels does not
    preserve the hue. Raising the solve margin made it worse, which is what
    finally identified the cause.

- id: A11
  title: brand assets
  owner: kit
  scripts: []
  needs: source
  severity: blocking
  status: planned
  booked: P3.1
  battery: false
  note: >-
    make_icon.py --check exists and is correct; what is missing is the gate
    that also checks the registry count, the PNG sizes and the website pins.

- id: A12
  title: EQS structure
  owner: kit
  scripts: [scripts/verify_eqs.py]
  needs: built
  severity: warn
  ramp: >-
    Blocking per repo when that repo's EQS retrofit is content-complete, and
    blocking from unit 1 on every new course. Existing content is never
    retro-failed on a content gate.
  status: partial
  gap: >-
    Checks the shortcode accessibility contracts. Does not yet check step order,
    budgets, vocab row count, input flood, pitfalls, or Reflect-to-implements
    correspondence. Those read heading ANCHORS, never heading text — efl, fle
    and daf write their steps in three languages and a checker keyed on English
    prose would reject two courses entirely.
  battery: true

- id: A13
  title: assessment arithmetic
  owner: kit
  scripts: [scripts/verify_assessment.py]
  needs: source
  severity: blocking
  status: live
  battery: true
  note: >-
    Reads the BODY, not the front matter. No exam page in this organisation
    carries total_points or notenschluessel, so a gate written against those
    fields would have examined nothing across 336 exams and reported success.
    The data is where the author wrote it: a stated total, per-task BE
    headings, and a grading-scale callout.

    Verifies 168 efl papers: tasks sum to the stated total, and the
    Notenschlüssel partitions 0…total with no gap and no overlap. A gap means a
    pupil with that mark has no grade; an overlap means they have two.

    Twelve Kl 13 papers are declared exam_scope: rehearsal — they drill one
    section of the 90-BE Klausur and state no total of their own. Declared, not
    inferred from prose: inferring would let a genuinely incomplete paper hide
    behind a keyword.

- id: A15
  title: inclusion and sourcing
  owner: kit
  scripts: [scripts/verify_inclusion.py]
  needs: source
  severity: blocking
  status: partial
  gap: >-
    Half of EQS 5.5, and the half that is missing is missing for a stated
    reason. Sourcing and speaker-name consistency are checked across all 396
    units. Gender share and cast-fact consistency are NOT, because they need a
    declared cast with declared attributes and no repo has one — the audio
    manifests carry file, label and transcript and nothing else. Inferring
    gender from a first name would be unreliable in German and French and
    inappropriate in any language, so the gate reports the absence rather than
    manufacturing a number. The fix is a cast declaration, which is authoring.
  battery: true

- id: A14
  title: differentiation
  owner: kit
  scripts: [scripts/verify_eqs.py]
  needs: built
  severity: warn
  ramp: as A12
  status: partial
  gap: >-
    The niveau marker contract is checked. The three-variant rule, the
    output_words bands, strictly increasing openness and the no-forked-input
    rule are not.
  battery: true

- id: A16
  title: conformance resolution
  owner: curriculum
  scripts: [scripts/conformance_audit.py]
  needs: source
  severity: warn
  ramp: >-
    Follows A2's milestone. Its thresholds are read from the pinned framework
    checkout and never from a constant, because F8 re-derives two scales and
    every coverage number in the programme moves when it lands.
  status: live
  battery: false
  runs: course-build.yml, against the pinned curriculum checkout

- id: A17
  title: review record
  owner: kit
  scripts: []
  needs: source
  severity: blocking
  status: planned
  booked: S6
  battery: false
  note: >-
    The 24-hour separation between G1 and G2 is read from git log, not from the
    front-matter date strings. Two dates a author typed in one sitting are not
    two reviews.

- id: A18
  title: marked non-unit page contract
  owner: kit
  scripts: [scripts/verify_page_contract.py]
  needs: source
  severity: blocking
  status: live
  battery: true
  note: >-
    EQS-1 governs units and says nothing about the other 89 marked pages in
    this organisation — glossaries, appendices, about pages. They carry
    registered marks exactly as units do and nothing examined them at all.

    It fails, and the findings are real: 20 marked pages below VG Wort's 1,800
    character Mindestumfang, 11 marked pages whose page_type is section, 9 with
    no author, and 22 fle pages that carry a mark while saying the content is
    still to come.

    The sources requirement is a WARNING, not a failure. Whether a page owes
    sources depends on whether it makes claims about the world, and a gate
    cannot tell a glossary of the course's own terminology from an appendix
    summarising external facts. Failing 48 pages on that distinction would
    teach people to stop reading the output.

- id: A19
  title: shortcode contract
  owner: kit
  scripts: [scripts/verify_shortcode_contract.py]
  needs: source
  severity: blocking
  status: live
  battery: true
  note: >-
    Checks three things: raw markup on a class a CONTENT shortcode owns, a
    shortcode used but not declared in kit/shortcodes.yml, and a declared
    shortcode whose LaTeX emitter symbol no .sty defines. The third found two
    on its first run — callout and details declared emitters that do not
    exist, because the emitter relocates those blocks rather than typesetting
    them.

    Content and furniture are separated, and the line was measured not argued:
    built and checked, a heading inside a raw <div> reaches .TableOfContents
    and the identical heading inside a shortcode does not. Converting daf's 65
    card blocks would drop 60 unit headings out of five landing-page outlines
    to satisfy a rule that protects the printed worksheet — which furniture
    never reaches, because the emitter globs unit content only.

# ── Stage B · build ──────────────────────────────────────────────────────────

- id: B1
  title: module graph
  owner: kit
  scripts: []
  needs: source
  severity: blocking
  status: planned
  booked: P3.1
  battery: false

- id: B2
  title: hugo --panicOnWarning
  owner: kit
  scripts: []
  needs: source
  severity: blocking
  status: live
  battery: false
  runs: course-build.yml build step — the gate is the flag, not a script
  note: >-
    This is why P0.4 had to retire the deprecated data API first. The flag and
    a deprecation warning together hard-fail every site.

- id: B3
  title: search index
  owner: kit
  scripts: [scripts/verify_pagefind.py]
  needs: built
  severity: blocking
  status: live
  battery: true
  opt_in: pagefind

- id: B4
  title: relative-baseURL rebuild
  owner: kit
  scripts: []
  needs: built
  severity: blocking
  status: planned
  booked: P3.1
  battery: false
  note: fle and daf each invented this separately; it belongs in one place.

# ── Stage C · rendered ───────────────────────────────────────────────────────

- id: C1
  title: marks registry integrity
  owner: kit
  scripts: [scripts/vgwort_audit.py]
  needs: source
  severity: warn
  ramp: >-
    Blocking on efl and daf from kit v1.1.0 — their dates are in-repo. Warn on
    fle for the whole programme and blocking at 15.4, because fle's 349
    front-matter marks carry no registered_at anywhere: the dates exist only in
    T.O.M. The sentinel unknown-pre-programme is accepted ONLY for codes already
    in the provisional lock at the P0.8 freeze, never for one assigned after.
  status: partial
  gap: hex/uniqueness/date-field checks not yet separated from the coverage walk
  battery: true

- id: C2
  title: one mark, one page, right page
  owner: kit
  scripts: [scripts/verify_rendered_pixels.py]
  needs: built
  severity: blocking
  status: live
  battery: true
  note: >-
    Asserts PRESENCE, where A3 asserts stability. A3 does catch a mark that
    stops rendering — verified against a deliberately broken daf, exit 1 with
    each code reported VANISHED — so this is a second, differently-shaped
    assertion rather than the only net: C2 checks that each locked code renders
    on exactly one page and on the page it is locked to.

    It spent its whole life until now resolving its lock from __file__, so it
    read the kit's vgwort/ directory while standing in a course, crashed with
    FileNotFoundError and returned non-zero. That looks like a failing gate and
    is an absent one. It also read a pixel_url column the lock has never had.

- id: C3
  title: no mark on hub, paginated or legal pages
  owner: kit
  scripts: [scripts/verify_pixel_delivery.py]
  needs: built
  severity: blocking
  status: live
  battery: true

- id: C4
  title: Mindestumfang coverage
  owner: kit
  scripts: [scripts/verify_vgwort_coverage.py]
  # built, not source. C4 counts RENDERED prose — shortcodes expand, partials
  # contribute — and a needs:source C4 that walked an absent public/ would
  # report zero uncovered pages and pass. It was needs:source for its whole
  # life, which is half of why it never examined a site.
  needs: built
  severity: warn
  ramp: >-
    Stays warn on existing pages permanently and is blocking on any NEW marked
    page. Registration is asynchronous — a page can be complete and correct for
    weeks before its mark exists — so a blocking coverage gate would punish the
    author for VG Wort's queue. That is the one ramp in this file with no end
    date, and the asymmetry is the reason.
  status: live
  battery: true

- id: C5
  title: pixel delivery shape
  owner: kit
  scripts: [scripts/verify_pixel_delivery.py]
  needs: built
  severity: blocking
  status: live
  battery: true
  note: >-
    A2 and C2 prove a mark is PRESENT. VG Wort counts a request, not a tag, and
    every failure here produces a page that looks correct in a diff, renders
    correctly in a browser, and earns nothing: loading="lazy" on a 1x1 pixel
    positioned off-screen is exactly what a lazy loader declines to fetch; a
    display:none ancestor stops the fetch where visibility:hidden does not; a
    script-injected pixel does not exist for a reader with JS off.

- id: C6
  title: rendered length floor
  owner: kit
  scripts: [scripts/verify_page_contract.py]
  needs: source
  severity: blocking
  status: live
  battery: true

- id: C7
  title: rendered placeholders
  owner: kit
  scripts: [scripts/verify_legal_placeholders.py]
  needs: built
  severity: blocking
  status: live
  battery: true

- id: C8
  title: author meta
  owner: kit
  scripts: [scripts/verify_author_meta.py]
  needs: built
  severity: blocking
  status: live
  battery: true

- id: C9
  title: analytics domain matches params
  owner: kit
  scripts: [scripts/verify_qa_basics.py]
  needs: built
  severity: blocking
  status: partial
  gap: replaces five separate greps; the domain-vs-params comparison is not in it yet
  battery: true

- id: C10
  title: sitemap, feed, robots, 404
  owner: kit
  scripts: [scripts/verify_qa_basics.py]
  needs: built
  severity: blocking
  status: live
  battery: true

- id: C11
  title: font coverage
  owner: kit
  scripts: []
  needs: built
  severity: blocking
  status: planned
  booked: P3.1
  battery: false
  note: >-
    Every rendered codepoint covered by a declared unicode-range AND present in
    that face's cmap. The two halves are different bugs: a range that does not
    cover the character, and a range that claims it while the cmap lacks it.

- id: C12
  title: RTL and language islands
  owner: kit
  scripts: [scripts/verify_eqs.py]
  needs: built
  severity: blocking
  status: partial
  gap: the tl lang check is live; dir, forced-LTR code blocks and inline style physical properties are not
  battery: true

- id: C13
  title: internal links
  owner: kit
  scripts: []
  needs: built
  severity: blocking
  status: planned
  booked: P3.1
  battery: false
  note: lychee --offline. External links are advisory and weekly, never here.

- id: C14
  title: accessibility sweep
  owner: kit
  scripts: []
  needs: built
  severity: blocking
  status: planned
  booked: P3.1
  note: >-
    scripts/pa11y.json exists and is correct, but a config is not a gate until
    something runs it — nothing does yet.
  staging: >-
    Two sweeps, staged deliberately. The one with ignore:[color-contrast] is
    blocking on adoption. The one with ignore:[] is warn until one release
    after BOTH S5 and F2, then blocking — because those two changes move every
    accent, the surface, the rule colour and the callout wash at once, and
    flipping contrast at the same moment produces a wall of violations with no
    way to separate a token bug from a content bug. The flip date is recorded
    in docs/release-policy.md so it is a commitment.
  battery: false

- id: C15
  title: JavaScript budget
  owner: kit
  scripts: [scripts/verify_js_budget.py]
  needs: built
  severity: blocking
  status: live
  battery: true
  opt_in: network_graph

# ── Stage D · artefacts ──────────────────────────────────────────────────────

- id: D1
  title: downloads resolve, no orphans
  owner: kit
  scripts: [scripts/verify_downloads.py]
  needs: materials
  severity: blocking
  status: partial
  gap: >-
    Checks that every referenced artefact exists. Does NOT yet check the
    reverse — an orphan PDF left behind by a deleted unit. No version of this
    check in the org has ever looked in that direction.
  battery: true

- id: D2
  title: PDF metadata
  owner: kit
  scripts: [scripts/verify_pdf_metadata.py]
  needs: materials
  severity: blocking
  status: live
  battery: true
  note: >-
    The predecessor greped /Author only, and 432 PDFs shipped with no /Title
    while it reported success. All five fields are checked now.

- id: D3
  title: thumbnails
  owner: kit
  scripts: []
  needs: materials
  severity: blocking
  status: planned
  booked: P3.2
  note: >-
    One renderer exists. The lock digest match and the 1000px ±2 width rule are
    not enforced yet; the 492 committed PNGs are recorded as standard:legacy
    and re-rendered when their source PDF next changes.
  battery: false

- id: D4
  title: print parity
  owner: kit
  scripts: []
  needs: materials
  severity: blocking
  status: planned
  booked: P3.4b
  battery: false

- id: D5
  title: glyph coverage
  owner: kit
  scripts: []
  needs: materials
  severity: blocking
  status: planned
  booked: F5 follow-up
  battery: false
  note: >-
    XeLaTeX prints "Missing character" and exits 0. The current builder checks
    only that a file appeared, so it ships tofu as a success. Two halves:
    pre-flight against the cmap, post-flight for glyph id 0 and U+FFFD.

- id: D6
  title: audio manifest and voice audition
  owner: kit
  scripts: [scripts/verify_voices.py]
  needs: source
  severity: blocking
  status: partial
  gap: >-
    The AUDITION half is implemented — status ready requires an audition file
    with a pass verdict, a named listener and a date, and a passed audition
    with a non-ready status is a disagreement rather than a pass. The MANIFEST
    half is not: manifest-to-disk both ways, transcript non-empty, source in
    {tts,human}, one channel at 64 kbit/s.

    It fails today on two counts and both are real.

    No audition: efl, fle and daf are all marked status: ready and none has an
    audition file, while between them they ship 323 audio manifests. Somebody
    has to listen.

    PROVENANCE: all five voices this organisation has selected — including all
    three that have shipped audio — are fine-tuned from en_US-lessac-medium,
    whose training corpus is non-commercial only and non-sublicensable. Each
    advertises an allowlist-clean DATASET licence, so the registry's licence
    check passed every one of them; the encumbrance is in the model card's
    Training line, which the registry had no field for. A sweep of the upstream
    catalogue found 76 of 175 voices in the same position.
  battery: true
  note: >-
    Licences are matched AFTER normalising. The registry spells one licence
    three ways — CC-BY 4.0, CC-BY-4.0 and a bare creativecommons.org URL — and
    an exact-string allowlist flagged seven codes of which four are fine. A
    gate that fails a course over a hyphen teaches people to ignore it.

# ── Stage E · deploy ─────────────────────────────────────────────────────────

- id: E1
  title: deploy shape
  owner: .github
  scripts: []
  needs: built
  severity: blocking
  status: live
  battery: false
  runs: >-
    course-build.yml — main only, upload-pages-artifact@v3 into deploy-pages@v4.
    Pull requests run A+B+C+D and deploy nothing. Enforced by the workflow's own
    structure and by the org audit's no-write rule, not by a script.
